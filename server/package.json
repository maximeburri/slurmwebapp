{
  "name": "slurmwebapp-server",
  "version": "0.0.0",
  "description": "Server bridge WebSocketSSL <-> SSH for a webapp client to manage a SLURM cluster ",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Maxime BURRI",
  "license": "ISC",
  "dependencies": {
    "express": "^4.13.3",
    "gulp": "^3.9.1",
    "gulp-cli": "^1.2.1",
    "shell-escape": "^0.2.0",
    "socket.io": "^1.4.5",
    "ssh2": "^0.4.12"
  }
}
